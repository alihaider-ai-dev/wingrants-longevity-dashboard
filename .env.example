# When running outside Streamlit Cloud (e.g. `streamlit run` locally with
# `--server.headless=true`), python-dotenv loads these as a fallback for
# the `st.secrets` lookups. Streamlit Cloud reads secrets.toml directly,
# so deploys don't touch this file.

APP_PASSWORD=REPLACE_ME_with_a_long_random_string
DB_URL=postgresql://wg_dashboard_ro:REPLACE_ME@host.eu-central-1.rds.amazonaws.com:5432/wingrants?sslmode=require
AWS_ACCESS_KEY_ID=REPLACE_ME
AWS_SECRET_ACCESS_KEY=REPLACE_ME
AWS_REGION=eu-central-1
AWS_S3_BUCKET=wingrants-results
